{
  "rules": {
    "teams": {
      "team": {
        ".read": true,
        ".write": "(!data.exists() || !newData.exists()) || auth.token.email === data.child('email').val() || (!data.)"
      }
    }
  }
}
